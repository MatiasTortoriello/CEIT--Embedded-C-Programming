/* Template program */

#include <stdio.h>

int main(){
	
	/* %d: integers
	 * %f: float 
	 * %u: unsigned
	 * %x: hexa
	 * */

	unsigned long int a = sizeof(int), b, c;
	printf("Size of int: %ld\n", sizeof(int));
	printf("Size of a: %ld\n", a);


	return 0;
}
